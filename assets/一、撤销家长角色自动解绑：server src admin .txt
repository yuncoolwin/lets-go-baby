一、撤销家长角色自动解绑：server/src/admin/admin.service.ts 的 revokeRole 方法，在 user_roles 更新为 inactive 成功之后、writeAuditLog 之前新增：当 target.role_type === 'parent' 时执行 delete parent_child_relations where parent_role_id = userRoleId，删除失败则抛错中断（错误信息"解除绑定失败: 原始信息"）；删除成功后把解绑条数写进本次 writeAuditLog 的 detail，格式为 { role_type: target.role_type, unbind_count: 条数 }；controller 和前端不动

- 二、修复清空字段保存无效：src/pages/admin/child-detail/index.tsx 的 handleSaveChild 中 payload 里五个字段改为直接传 trim 后的值、去掉 || undefined 兜底——allergies: editAllergies.trim()、nickname: editNickname.trim()、parent_name: editParentName.trim()、parent_phone: editParentPhone.trim()、health_info: editHealthInfo.trim()；后端不动

- 三、教师班级多选：
- server/src/storage/database/shared/schema.ts：新增 teacherClasses 表定义，字段 id varchar(36) default gen_random_uuid() 主键、teacher_id varchar(36) notNull 外键关联 teachers.id、class_id varchar(36) notNull 外键关联 classes.id、created_at timestamptz defaultNow() notNull，加 unique(teacher_id, class_id) 约束
- server/src/teachers/teachers.service.ts：findAll 在现有返回基础上批量查 teacher_classes（in teacher_id），给每个教师附 class_ids 数组和 class_names 数组（names 用 classes 表 in id 查），现有 class_name 单值字段保留；create 和 update 的 dto 增加 class_ids?: string[]，create 时 teachers.class_id 写 class_ids[0] 并批量插入关联行（去重），update 时先删该教师现有关联中不在新列表的行、再插入缺少的行，teachers.class_id 同步为 class_ids[0]，class_ids 为空数组时清空关联和 class_id
- server/src/auth/authz.service.ts 的 getTeacherClassIds：改为先按 user_id + active 查 teachers 得行数组，取 id 数组查 teacher_classes 的 class_id，与 teachers 行自身的 class_id 合并去重后返回
- server/src/teacher/teacher.service.ts：getMe 返回数据补 class_ids（由 teachers 行 id 查 teacher_classes，并上该行 class_id 去重）；getGroupedOverview 改为取该教师全部班级 class_ids 后逐班调用现有 getGroupedOverviewByClass(classId, date)，每组结果附加 class_name 字段后按顺序拼接返回，无数据的班级跳过；getTeacherClasses 改为返回该教师全部班级（关联表结果与 class_id 并集）的 id 和 name 列表
- server/src/children/children.service.ts 的 findAll：在现有按 teachers 行 class_id 构建 teachersMap 的基础上，再查 teacher_classes（in teachers.id），对每条关联行把该教师姓名 push 进对应班级数组，同一班级同名去重
- server/src/notifications/notifications.service.ts：检查所有用 teachers.class_id 过滤或分组教师的查询（约 436 行附近为主），过滤范围改为 teachers.class_id 与 teacher_classes 关联结果的并集
- src/pages/admin/teacher-edit/index.tsx：formData 的 class_id 单值改为 class_ids: string[]，编辑回显时取接口返回的 class_ids（没有则用 [class_id] 过滤空值）；handleClassToggle 改为多选切换（已选中则从数组移除、未选中则加入）；提交 payload 里 class_id 改为传 class_ids 数组
- src/pages/admin/teacher-manage/index.tsx：Teacher 接口补 class_ids?: string[] 和 class_names?: string[]，卡片班级行改为遍历 class_names 逐个渲染标签，数组为空时不显示该行

- 四、操作日志详细化：
- server/src/children/children.service.ts：logAudit 增加 changes?: string[] 参数并写入 detail（{ name, changes }）；update 方法内定义字段中文映射 name=姓名、nickname=小名、gender=性别、birth_date=出生日期、class_id=班级、parent_name=家长姓名、parent_phone=家长电话、health_info=健康信息、allergies=过敏情况、status=在读状态、course_type=课程类型、enrollment_duration=报读时长、start_date=开始日期、end_date=结束日期、payment_amount=缴费金额、payment_channel=缴费渠道、payment_status=缴费状态，按 updateData 实际更新的键生成 changes 数组传给 logAudit
- server/src/admin/admin.service.ts：role_assign 日志（assignRole）补 detail { name: 目标用户nickname, role_type: roleType }，nickname 由 user_roles.user_id 查 users 获得；revokeRole 撤销前查目标角色对应用户的 nickname，日志 detail 改为 { name: nickname, role_type: target.role_type }（与第一项的 unbind_count 合并写入）；deletePermissionUser 删除前查 users.nickname，日志 detail 改为 { name: nickname }；createPermissionUser 日志 detail 改为 { name: created.nickname }；updatePermissionUser 日志 detail 改为 { name: 更新后nickname }
- src/pages/admin/audit-logs/index.tsx：ACTION_LABELS 中 user_create 改为"新增了用户「{name}」"、user_update 改为"编辑了用户「{name}」"、user_delete 改为"删除了用户「{name}」"、role_assign 改为"给「{name}」分配了{role}"、role_revoke 改为"撤销了「{name}」的{role}"、child_update 改为"编辑了幼儿「{name}」的{changes}"；buildPhrase 内新增映射 ROLE_LABELS = { superadmin: '超管角色', admin: '管理员角色', teacher: '教师角色', parent: '家长角色' }，{role} 用 ROLE_LABELS[detail.role_type] 替换、detail 无 role_type 时替换为"角色"，{changes} 用 (detail.changes || []).join('、') 替换、空数组时替换为"信息"；name 为空时的降级：user_create 显示"新增了用户"、user_update 显示"编辑了用户"、user_delete 显示"删除了用户"、role_assign 显示"分配了{role}"、role_revoke 显示"撤销了{role}"、child_update 显示"编辑了幼儿"